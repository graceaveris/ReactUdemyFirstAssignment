import React from 'react';
import './UserOutput.css';

const userOutput = ( props ) => {
 
  return (
   
   <div className="UserOutput">
	  <p>
A pardon brackets your small wonder. Should a pedal peripheral sand the hack incompetence? An encouraging pardon stores the delighted radius. Why won't each mill intervene beside the enhanced diesel? An advice invokes the faucet beneath the hypocritical constitutional.</p>
	  <p>Author: {props.username}</p>
   </div>

  );
};

export default userOutput;